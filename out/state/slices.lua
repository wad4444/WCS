-- Compiled with roblox-ts v2.2.0
local TS = _G[script]
local replicationSlice = TS.import(script, script.Parent, "slicesContainer", "replication").replicationSlice
local slices = {
	replication = replicationSlice,
}
return {
	slices = slices,
}
